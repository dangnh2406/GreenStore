<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.poly.appchatonline">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="StoreGreen"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoreGreen">
        <activity android:name="vn.poly.storegreen.ForgotActivity"
            android:parentActivityName="vn.poly.storegreen.StartActivity"
            ></activity>
        <activity
            android:name="vn.poly.storegreen.activity.LoadScreenActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="vn.poly.storegreen.LoginActivity"
            android:parentActivityName="vn.poly.storegreen.StartActivity" />
        <activity
            android:name="vn.poly.storegreen.activity.HomeActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name="vn.poly.storegreen.MainActivity" />
        <activity
            android:name="vn.poly.storegreen.RegisterActivity"
            android:parentActivityName="vn.poly.storegreen.StartActivity" />
        <activity android:name="vn.poly.storegreen.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>